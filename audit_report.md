# تقرير تدقيق نظام Challengawy HR BOT

يعرض هذا التقرير تفصيلاً للأخطاء البرمجية، والثغرات الأمنية، والمشاكل المنطقية، ومشاكل الأداء، واستهلاك الموارد التي تم العثور عليها خلال المراجعة الشاملة للنظام (Cloudflare Workers, D1 SQLite, Telegram Bot, Vue.js Dashboard).

---

## 1. الأخطاء والثغرات الأمنية (Security Vulnerabilities)

1. **غياب التحقق وتطهير مدخلات المستخدم (Missing Input Validation & Sanitization):**
   - في العديد من نقاط النهاية مثل `/api/admin/employees` و `/api/admin/holidays`، يتم استقبال الـ `JSON payload` وقراءته كـ `any` وتمريره مباشرة إلى استعلامات قاعدة البيانات `INSERT/UPDATE` دون التأكد من نوع البيانات أو حدودها (مثال: يمكن إدخال راتب `base_salary` بالسالب، أو نصوص طويلة جداً تسبب إرهاقاً للذاكرة).
   - **الحل:** استخدام مكتبات مثل `Zod` للتحقق الصارم من هيكل البيانات قبل تنفيذ أي استعلام.

2. **تسريب بيانات الـ Telegram Auth وإمكانية إعادة الاستخدام (Replay Attacks):**
   - في مسار المصادقة `/api/auth/telegram`، ورغم وجود شرط لضمان ألا يتجاوز وقت المصادقة 24 ساعة، إلا أنه لا توجد آلية تمنع المهاجم من إعادة إرسال نفس حزمة البيانات الناجحة (`Replay Attack`) بشكل متكرر لاستخراج `JWT` جديد طوال فترة الـ 24 ساعة.
   - **الحل:** حفظ قيمة الـ `hash` في قاعدة البيانات وتحديدها كـ "مُستخدمة" (Used/Nonce) بمجرد تسجيل الدخول للمرة الأولى.

3. **غياب تحديد معدل الطلبات (Rate Limiting):**
   - النظام لا يحتوي على أي حماية ضد الهجمات العشوائية أو استنزاف الموارد، ولا يوجد Rate Limiting على روابط الـ API، ما يمكن أن يسبب تجاوز حدود Cloudflare Workers المجانية بسهولة ويؤدي لتعطل البوت.

---

## 2. الأخطاء المنطقية في الأعمال (Business Logic Flaws)

1. **خلل كارثي في حساب الرواتب (Payroll Calculation):**
   - **السبب:** في ملف `backend/api/admin.ts` وتحديداً في `POST /api/admin/payroll/issue`، يتم استخراج معدل الراتب اليومي بافتراض أن الشهر دائماً 30 يوماً (`dailyRate = base_salary / 30`). ثم يتم ضرب هذا المعدل في أيام الشهر الفعلية `daysInMonth`!
   - **النتيجة:** في الشهور ذات الـ 31 يوماً، سيحصل الموظف على راتب يفوق راتبه الأساسي بالكامل!
   - **الحل:** الراتب الأساسي `base_salary` يجب أن يكون ثابتاً بغض النظر عن أيام الشهر، وتستخدم الـ `daysInMonth` فقط لحساب الخصومات إذا لزم الأمر.

2. **خصم السلف وتصفيرها دون مراعاة حدود الراتب:**
   - عند إصدار الراتب، يقرأ النظام `activeLoan` ويخصمها مباشرة من الراتب الصافي باستخدام `Math.max(0, dynamicMonthSalary - totalDed)`.
   - **المشكلة:** إذا كانت السلفة أكبر من راتب الموظف، سيصبح راتب الموظف 0، **ولكن** سيقوم النظام باستدعاء `markEmployeeLoansAsPaid()` ليجعل جميع السلف "مُسددة"، مما يؤدي لضياع مبالغ السلفة المتبقية التي لم يغطها الراتب.

3. **غياب ضمان التزامن في قواعد البيانات (No Transactional Integrity):**
   - يتم إصدار الراتب ثم تغيير حالة السلفة في عمليتين منفصلتين:
     ```typescript
     await issuePayroll(...);
     if (activeLoan > 0) await markEmployeeLoansAsPaid(...);
     ```
   - إذا تعطل الخادم أو توقف التنفيذ بعد إصدار الراتب وقبل سداد السلفة، سيتم إصدار الراتب بخصم مبلغ السلفة، وستظل السلفة مسجلة كـ `pending/approved` مما يعني أنها ستخصم مجدداً الشهر التالي.
   - **الحل:** توحيدها في عملية `D1 Batch Transaction`.

---

## 3. الأداء العالي واستهلاك الموارد (Performance & Resource Bottlenecks)

1. **مشكلة (N+1 Query) وانقطاع التنفيذ (Timeouts):**
   - في مسار الإصدار الجماعي للرواتب `/api/admin/payroll/issue`، وفي بث الرسائل `/api/admin/broadcast`، يتم عمل حلقة `for...of` فوق كل الموظفين:
     ```typescript
     for (const employee of employees) {
         await db.query(...);
         await fetch(telegram_api);
     }
     ```
   - هذا النمط سيؤدي لتنفيذ آلاف الطلبات المتسلسلة. مع بطء الاتصال الخارجي أو استعلامات D1، سيتجاوز הـ Worker الحد الأقصى لوقت التنفيذ في Cloudflare (غالباً 10-30 ثانية) مما سيؤدي لفشل العملية بشكل صامت، فلن يستلم نصف الموظفين رسائلهم، أو رواتبهم.

2. **استهلاك الذاكرة (Memory Exhaustion - OOM) في تصدير البيانات (Export):**
   - في ملف `backend/api/export.ts`، يتم سحب جميع البيانات عبر استعلام `SELECT * ... .all()` مرة واحدة، ثم يتم معالجتها لإنشاء ملف `Excel` في الذاكرة (Memory Buffer).
   - بمجرد زيادة بيانات الموظفين أو الحضور عن بضعة آلاف سجل، سيتجاوز الـ Buffer حد الـ 128MB المخصصة في الـ Worker مما سيتسبب بسقوط الخادم و `Error 1101` من Cloudflare.
   - **الحل:** يجب استخدام تقنيات (Streams/Pagination) لإنتاج التقارير الضخمة، أو ترحيل العملية لتتم عبر `Cron Jobs` وإرسال رابط التنزيل لاحقاً.

3. **انعدام التقسيم (No Pagination):**
   - واجهة `GET /api/employees` واستعلامات الحضور تعيد البيانات دفعة واحدة، وبعضها مقيد بشكل غير مرن `LIMIT 100`. لاحقاً سيمنع هذا التقييد المديرين من رؤية السجلات القديمة نهائياً لعدم وجود معمارية `Page` أو `Cursor` حقيقية.

---

## 4. الأخطاء البرمجية (Programming Bugs)

1. **معالجة خاطئة للأخطاء في الـ Webhook:**
   - في `webhook.ts`:
     ```typescript
     catch (err: any) {
       console.error('[Webhook Error]', err);
       return new Response('OK', { status: 200 });
     }
     ```
   - إرجاع `200 OK` يخفي الأخطاء عن تلغرام، وهذا ممارسة مقبولة لمنع إعادة إرسال الرسالة بشكل لانهائي، ولكنه يخفي أي `Crash` يحدث داخل البوت ويجعل المراقبة مستحيلة من دون نظام تتبع أخطاء مخصص (مثل Sentry).

2. **الاعتماد الكثيف على الـ `any` (Loss of Type Safety):**
   - غالبية قراءة البيانات من طلبات الـ HTTP تمت برمجتها عبر `as any`، مما يُفقد قوة لغة `TypeScript` في اصطياد الأخطاء قبل الـ Runtime، ويجعل الكود هشّاً وعرضة لـ `TypeError`.

3. **تجاوز إجازات متعددة لنفس اليوم (Concurrency Bug):**
   - في دالة فحص تداخل الإجازات `hasOverlappingLeave` يتم التحقق قبل الإدراج. ولكن لعدم وجود قفل (Lock/Mutex)، إذا قام الموظف بإرسال طلبين في نفس الجزء من الثانية، سيجتاز كلاهما التحقق، ويتم تسجيل إجازتين متقاطعتين.
